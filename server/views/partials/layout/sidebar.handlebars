

<aside
    class="sidenav navbar navbar-vertical navbar-expand-xs border-0 border-radius-xl my-3 fixed-start ms-3 ps ps--active-y"
    id="sidenav-main">
    {{!-- Header --}}
    {{!-- Header --}}
    {{!-- Header --}}
    <div class="sidenav-header">
        {{!-- close button for sidenav --}}
        <i class="fas fa-times p-3 cursor-pointer text-secondary opacity-5 position-absolute end-0 top-0 d-xl-none"
            aria-hidden="true" id="iconSidenav"></i>
        {{!-- logo --}}
        <a class="navbar-brand m-0" href="/">
            <img src="">
            {{!-- Username --}}
            {{!-- Username --}}
            <span class="ms-1 text-uppercase font-weight-bold">Pokemon Types Helper</span>
        </a>
    </div>
    <hr class="horizontal dark mt-0">
    {{!-- Body --}}
    {{!-- Body --}}
    {{!-- Body --}}
    <div class="collapse navbar-collapse w-auto ps ps--active-y" id="sidenav-collapse-main">

        <ul class="navbar-nav">
            {{!-- Title --}}
            {{!-- Title --}}
            <li class="nav-item">
                <h6 class="ps-4 ms-2 text-uppercase text-xs font-weight-bolder opacity-6">Types</h6>
            </li>
            {{!-- Completed forms --}}
            {{#each typesList as |type i|}}
            <li class="nav-item">
                <a class="nav-link" href="/type/{{type.name}}">
                    {{!-- <i class="fa-solid fa-clipboard-check"></i> --}}
                    <span class="nav-link-text ms-1">{{type.name}}</span>
                </a>
            </li>
            {{/each}}
        </ul>
    </div>

</aside>